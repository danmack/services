#!/bin/sh

# Installing Foreman and Puppet on Centos 7

sudo yum update
sudo yum upgrade

sudo systemctl disable firewalld
sudo systemctl stop firewalld

sudo hostnamectl set-hostname foreman.onsrud.home

sudo echo "10.10.10.117 foreman.onsrud.home" >> /etc/hosts

sudo yum install -y chrony

sudo timedatectl set-timezone America/Chicago

sudo systemctl enable --now chronyd

sudo yum install -y https://yum.puppet.com/puppet6-release-el-8.noarch.rpm

sudo yum install -y https://yum.theforeman.org/releases/2.1/el8/x86_64/foreman-release.rpm

sudo yum install -y foreman-installer

sudo foreman-installer
